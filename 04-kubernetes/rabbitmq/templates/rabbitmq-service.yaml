apiVersion: v1
kind: Service
metadata:
  name: '{{ include "rabbitmq.fullname" . }}'
spec:
  selector:
    app: '{{ include "rabbitmq.fullname" . }}'
  ports:
  - port: 5672
    targetPort: 5672
    name: amqp
  - port: 15672
    targetPort: 15672
    name: mgmt
